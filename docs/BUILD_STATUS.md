# Build status

Status: release-candidate preview. The app, launch corpus, tests, validators, hosting configuration, and preview safeguards are implemented. Automated results and deployed verification are recorded in `QA_REPORT.md`; owner-controlled production identity and domain values remain intentionally unset.

The preview is not a production launch. It must remain noindex, must not emit a production canonical, and must not be promoted as clinically validated or medically reviewed.
